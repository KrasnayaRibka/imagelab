This microservice is designed for fast upload, processing, and subsequent copying of images to the target order folder. It communicates with other parts of the system via RabbitMQ. It also exposes web endpoints for uploading and copying images.

The service is meant to run in a specific environment; it is not intended to be used on its own. This repository is published publicly only as a demonstration of my coding style and capabilities.
